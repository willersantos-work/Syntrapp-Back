interface CreateSAFSpaceDTO {
        space_id: string;
        name: string;
        area: number;
        lines_quantity: string;
        width_line: number;
        width_interline: number;
}

export default CreateSAFSpaceDTO;
